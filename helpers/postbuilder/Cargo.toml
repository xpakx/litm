[package]
name = "postbuilder"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
swc_core = { version = "57.0.1", features = ["common", "ecma_ast", "ecma_parser", "ecma_visit", "ecma_codegen"] }
walkdir = "2.5.0"
